# Core runtime dependencies. Install the optional OCR runtime from
# requirements-ocr.txt when Surya fallback is needed.
beautifulsoup4>=4.15.0,<5
PyMuPDF>=1.28.2,<2
psutil>=7.2.2,<8
requests>=2.34.2,<3
